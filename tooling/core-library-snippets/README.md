# Core library snippets
These snippets provides you the standard core component library components to use across multiple JS frameworks and libraries.

### Note
* As a pre-alpha, this works with html files only

